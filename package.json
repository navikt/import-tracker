{
  "name": "import-stats",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "yarn workspace app dev",
    "build": "yarn workspace app build",
    "test": "yarn workspace app test",
    "test:watch": "yarn workspace app test:watch"
  },
  "workspaces": [
    "website",
    "app"
  ],
  "devDependencies": {
    "@actions/core": "^1.5.0",
    "@actions/github": "^5.0.0",
    "lerna": "^4.0.0",
    "prettier": "^2.2.1",
    "prettier-plugin-tailwindcss": "^0.1.5"
  },
  "prettier": {}
}
